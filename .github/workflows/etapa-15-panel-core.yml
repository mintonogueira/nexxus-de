name: Etapa 15 - Panel Core

on:
  push:
    branches:
      - etapa-15-panel-core-impl
      - main
    paths:
      - 'etapas/etapa-15-panel-core/**'
      - '.github/workflows/etapa-15-panel-core.yml'
  pull_request:
    paths:
      - 'etapas/etapa-15-panel-core/**'
      - '.github/workflows/etapa-15-panel-core.yml'

jobs:
  rust:
    name: Rust ${{ matrix.name }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Arch Linux
            image: archlinux:latest
            install: pacman -Syu --noconfirm rust cargo
          - name: Debian Trixie
            image: debian:trixie
            install: apt-get update && apt-get install -y --no-install-recommends rustc cargo ca-certificates
    container:
      image: ${{ matrix.image }}
    defaults:
      run:
        working-directory: etapas/etapa-15-panel-core
    steps:
      - uses: actions/checkout@v4
      - name: Dependências
        run: ${{ matrix.install }}
      - name: Versões
        run: rustc --version && cargo --version
      - name: Format
        run: cargo fmt --all -- --check
      - name: Testes
        run: cargo test --workspace --all-targets
      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings
      - name: Rustdoc
        env:
          RUSTDOCFLAGS: -D warnings
        run: cargo doc --workspace --no-deps
